# 🏢 Emadara - سیستم مدیریت نظرسنجی سازمانی

یک سیستم جامع مدیریت نظرسنجی سازمانی با قابلیت مدیریت کاربران، سمت‌های سازمانی و نمودار سازمانی تعاملی.

## 📋 فهرست مطالب

- [معرفی](#معرفی)
- [ویژگی‌های کلیدی](#ویژگیهای-کلیدی)
- [تکنولوژی‌های استفاده شده](#تکنولوژیهای-استفاده-شده)
- [پیش‌نیازها](#پیشنیازها)
- [نصب و راه‌اندازی](#نصب-و-راهاندازی)
- [پیکربندی](#پیکربندی)
- [استفاده](#استفاده)
- [ساختار پروژه](#ساختار-پروژه)
- [API Documentation](#api-documentation)
- [مشارکت](#مشارکت)
- [لایسنس](#لایسنس)

## 🎯 معرفی

**Emadara** یک سیستم مدیریت نظرسنجی سازمانی است که به سازمان‌ها امکان می‌دهد:
- نظرسنجی‌های سفارشی ایجاد کنند
- ساختار سازمانی خود را مدیریت کنند
- نمودار سازمانی تعاملی و بصری داشته باشند
- کاربران و نقش‌های مختلف را مدیریت کنند
- گزارش‌های جامع از نظرسنجی‌ها دریافت کنند

این پروژه شامل یک **Backend** مبتنی بر NestJS و یک **Frontend** مبتنی بر Next.js است که با هم یک سیستم کامل و کاربردی را تشکیل می‌دهند.

## ✨ ویژگی‌های کلیدی

### 🔐 احراز هویت و مجوزها
- سیستم احراز هویت مبتنی بر JWT
- نقش‌های کاربری (Admin, Manager, Employee)
- مدیریت کاربران و پروفایل‌ها
- آپلود و مدیریت تصاویر پروفایل

### 📊 مدیریت نظرسنجی
- ایجاد و ویرایش نظرسنجی‌های سفارشی
- انواع مختلف سوالات (چند گزینه‌ای، متنی، رتبه‌بندی)
- تخصیص نظرسنجی به کاربران یا گروه‌ها
- ردیابی وضعیت تکمیل نظرسنجی‌ها

### 🏗️ مدیریت ساختار سازمانی
- تعریف سمت‌های سازمانی
- ساختار سلسله مراتبی مدیران و کارمندان
- نمودار سازمانی تعاملی و بصری
- مدیریت روابط گزارش‌دهی

### 📈 گزارش‌گیری و تحلیل
- داشبورد مدیریتی
- گزارش‌های تفصیلی نظرسنجی‌ها
- آمار و ارقام کلیدی

## 🛠️ تکنولوژی‌های استفاده شده

### Backend
- **Framework**: NestJS 11.x
- **Database**: Microsoft SQL Server (MSSQL)
- **ORM**: TypeORM
- **Authentication**: JWT (Passport)
- **Password Hashing**: bcrypt
- **Validation**: class-validator, class-transformer
- **File Upload**: Multer
- **Language**: TypeScript

### Frontend
- **Framework**: Next.js 16.x (App Router)
- **UI Library**: React 19.x
- **Styling**: Tailwind CSS 4.x
- **Charts & Visualization**: 
  - ReactFlow (نمودار سازمانی)
  - react-d3-tree (نمودار درختی)
  - D3.js
  - Dagre (چیدمان خودکار گراف)
- **Language**: TypeScript

## 📦 پیش‌نیازها

قبل از شروع، مطمئن شوید که موارد زیر را نصب کرده‌اید:

- **Node.js**: نسخه 20.x یا بالاتر
- **npm** یا **yarn**: مدیریت پکیج
- **Microsoft SQL Server**: برای دیتابیس
- **Git**: برای کلون کردن پروژه

## 🚀 نصب و راه‌اندازی

### 1. کلون کردن پروژه

```bash
git clone https://github.com/kamihadadg/emadara.git
cd emadara
```

### 2. نصب Backend

```bash
cd backend
npm install
```

### 3. نصب Frontend

```bash
cd ../frontend
npm install
```

### 4. پیکربندی دیتابیس

یک دیتابیس در SQL Server ایجاد کنید و اطلاعات اتصال را در فایل `.env` قرار دهید.

### 5. اجرای Migration‌ها

Backend به صورت خودکار جداول را در اولین اجرا ایجاد می‌کند (با استفاده از `synchronize: true` در TypeORM).

### 6. ایجاد کاربر Admin پیش‌فرض

در اولین اجرا، یک کاربر Admin به صورت خودکار ایجاد می‌شود:
- **نام کاربری**: `admin` (یا مقدار `ADMIN_USERNAME` در `.env`)
- **رمز عبور**: `admin123` (یا مقدار `ADMIN_PASSWORD` در `.env`)
- **شماره پرسنلی**: `ADMIN001`

⚠️ **توجه**: حتماً پس از اولین ورود، رمز عبور را تغییر دهید.

### 7. اجرای پروژه

#### Backend (Development Mode)
```bash
cd backend
npm run start:dev
```
Backend روی پورت `8081` اجرا می‌شود.

#### Frontend (Development Mode)
```bash
cd frontend
npm run dev
```
Frontend روی آدرس `http://192.168.1.112:8080` اجرا می‌شود.

## ⚙️ پیکربندی

### Backend Environment Variables

فایل `.env` را در پوشه `backend` ایجاد کنید:

```env
# Database Configuration
DB_HOST=localhost
DB_PORT=1433
DB_USERNAME=sa
DB_PASSWORD=your_password
DB_DATABASE=emadara

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
JWT_EXPIRATION=24h

# Admin User (Optional)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# Server Configuration
PORT=8081
```

### Frontend Configuration

فایل `package.json` در بخش `scripts` پیکربندی شده است:

```json
{
  "scripts": {
    "dev": "next dev -p 8080 -H 192.168.1.112"
  }
}
```

برای تغییر آدرس و پورت، این بخش را ویرایش کنید.

## 📖 استفاده

### ورود به سیستم

1. به آدرس `http://192.168.1.112:8080/login` بروید
2. با اطلاعات Admin وارد شوید
3. پس از ورود، به داشبورد مدیریتی منتقل می‌شوید

### مدیریت کاربران

1. از منوی Admin، گزینه "مدیریت کاربران" را انتخاب کنید
2. کاربران جدید اضافه کنید
3. سمت‌های سازمانی تعریف کنید
4. روابط گزارش‌دهی را مشخص کنید

### ایجاد نظرسنجی

1. از داشبورد، "نظرسنجی جدید" را انتخاب کنید
2. عنوان و توضیحات را وارد کنید
3. سوالات را اضافه کنید
4. گروه هدف را مشخص کنید
5. نظرسنجی را منتشر کنید

### مشاهده نمودار سازمانی

1. از منوی اصلی، "نمودار سازمانی" را انتخاب کنید
2. نمودار تعاملی سازمان را مشاهده کنید
3. روی هر کارمند کلیک کنید تا جزئیات را ببینید

## 📁 ساختار پروژه

```
emadara/
├── backend/                    # Backend (NestJS)
│   ├── src/
│   │   ├── auth/              # ماژول احراز هویت
│   │   │   ├── auth.controller.ts
│   │   │   ├── auth.service.ts
│   │   │   ├── auth.module.ts
│   │   │   ├── jwt.strategy.ts
│   │   │   └── dto/           # Data Transfer Objects
│   │   ├── survey/            # ماژول نظرسنجی
│   │   │   ├── survey.controller.ts
│   │   │   ├── survey.service.ts
│   │   │   ├── survey.module.ts
│   │   │   ├── entities/      # Entity‌های دیتابیس
│   │   │   └── dto/
│   │   ├── app.module.ts      # ماژول اصلی
│   │   ├── main.ts            # Entry point
│   │   ├── seed.ts            # Data seeder
│   │   └── admin-seeder.ts    # Admin user seeder
│   ├── uploads/               # فایل‌های آپلود شده
│   ├── package.json
│   └── tsconfig.json
│
├── frontend/                   # Frontend (Next.js)
│   ├── src/
│   │   ├── app/               # App Router pages
│   │   │   ├── login/         # صفحه ورود
│   │   │   ├── register/      # صفحه ثبت‌نام
│   │   │   ├── admin/         # داشبورد مدیریت
│   │   │   └── page.tsx       # صفحه اصلی
│   │   ├── components/        # کامپوننت‌های React
│   │   │   ├── InteractiveOrgChart.tsx
│   │   │   └── ...
│   │   ├── contexts/          # React Contexts
│   │   ├── lib/               # Utilities
│   │   └── types/             # TypeScript Types
│   ├── public/                # فایل‌های استاتیک
│   ├── package.json
│   └── tsconfig.json
│
├── .gitignore
├── LICENSE                     # MIT License
├── README.md                   # این فایل
└── reset-db.js                # اسکریپت ریست دیتابیس
```

## 🔌 API Documentation

### Authentication Endpoints

#### ثبت‌نام کاربر جدید
```http
POST /auth/register
Content-Type: application/json

{
  "employeeId": "EMP001",
  "username": "john.doe",
  "firstName": "جان",
  "lastName": "دو",
  "password": "securePassword123",
  "positionId": 1,
  "managerId": 2,
  "role": "EMPLOYEE"
}
```

#### ورود به سیستم
```http
POST /auth/login
Content-Type: application/json

{
  "username": "john.doe",
  "password": "securePassword123"
}

Response:
{
  "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "user": {
    "id": 1,
    "username": "john.doe",
    "role": "EMPLOYEE"
  }
}
```

#### دریافت اطلاعات کاربر
```http
GET /auth/me
Authorization: Bearer {access_token}
```

### User Management Endpoints

#### دریافت لیست کاربران
```http
GET /auth/users
Authorization: Bearer {access_token}
```

#### آپلود تصویر پروفایل
```http
POST /auth/upload-profile-picture
Authorization: Bearer {access_token}
Content-Type: multipart/form-data

{
  "file": [binary data]
}
```

### Position Management Endpoints

#### ایجاد سمت سازمانی
```http
POST /auth/positions
Authorization: Bearer {access_token}
Content-Type: application/json

{
  "title": "مدیر فروش",
  "description": "مسئول مدیریت تیم فروش"
}
```

#### دریافت لیست سمت‌ها
```http
GET /auth/positions
Authorization: Bearer {access_token}
```

### Survey Endpoints

#### ایجاد نظرسنجی
```http
POST /survey
Authorization: Bearer {access_token}
Content-Type: application/json

{
  "title": "نظرسنجی رضایت کارکنان",
  "description": "ارزیابی میزان رضایت کارکنان",
  "questions": [...]
}
```

#### دریافت لیست نظرسنجی‌ها
```http
GET /survey
Authorization: Bearer {access_token}
```

## 🤝 مشارکت

مشارکت شما در بهبود این پروژه بسیار ارزشمند است! برای مشارکت:

1. این مخزن را Fork کنید
2. یک Branch جدید ایجاد کنید (`git checkout -b feature/AmazingFeature`)
3. تغییرات خود را Commit کنید (`git commit -m 'Add some AmazingFeature'`)
4. Branch را Push کنید (`git push origin feature/AmazingFeature`)
5. یک Pull Request ایجاد کنید

### راهنمای کدنویسی

- از TypeScript برای تمام کدها استفاده کنید
- از ESLint و Prettier برای فرمت کد استفاده کنید
- کامنت‌های مناسب برای کدهای پیچیده بنویسید
- تست‌های مناسب برای ویژگی‌های جدید بنویسید

## 🐛 گزارش باگ

اگر باگی پیدا کردید، لطفاً یک Issue در GitHub ایجاد کنید و موارد زیر را ذکر کنید:

- توضیح واضح از باگ
- مراحل بازتولید باگ
- رفتار مورد انتظار
- اسکرین‌شات (در صورت امکان)
- اطلاعات محیط (نسخه Node.js، مرورگر، سیستم‌عامل)

## 📝 To-Do List

- [ ] افزودن تست‌های واحد و یکپارچه
- [ ] پیاده‌سازی نوتیفیکیشن‌های Real-time
- [ ] افزودن قابلیت Export گزارش‌ها (PDF, Excel)
- [ ] پیاده‌سازی چند زبانه (i18n)
- [ ] بهبود عملکرد و بهینه‌سازی
- [ ] افزودن داکیومنتیشن API با Swagger
- [ ] پیاده‌سازی Rate Limiting
- [ ] افزودن قابلیت Backup خودکار

## 📄 لایسنس

این پروژه تحت لایسنس MIT منتشر شده است. برای اطلاعات بیشتر فایل [LICENSE](LICENSE) را مطالعه کنید.

## 👤 نویسنده

**Kamran**

- GitHub: [@kamihadadg](https://github.com/kamihadadg)

## 🙏 تشکر

از تمام کسانی که در توسعه این پروژه مشارکت داشته‌اند، تشکر می‌کنیم.

---

**ساخته شده با ❤️ برای مدیریت بهتر سازمان‌ها**
